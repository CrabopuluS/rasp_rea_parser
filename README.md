# RЭУ расписание Telegram Bot

Асинхронный бот на `python-telegram-bot` для получения расписания rasp.rea.ru, экспорта в `.ics` и плановой отправки в чаты.

## Запуск
1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
2. Создайте файл `.env` (можно взять за основу `.env.example`) и задайте как минимум `TELEGRAM_BOT_TOKEN`.
3. Запустите бота:
   ```bash
   python bot.py
   ```

## Основные команды
- `/start` (`/help`) — приветствие и подсказки, клавиатура с кнопками.
- `/week [url] [group]` — текстовое расписание недели.
- `/ics [url] [group]` — два `.ics` файла (мобильный и Google).
- `/plan <YYYY-MM-DD> <HH:MM> [url] [group]` — плановая отправка по МСК.

Те же действия доступны по кнопкам: «📅 Расписание недели», «📂 Получить .ics», «⏰ Запланировать отправку». В личных чатах любой текст без команды трактуется как код группы. В группах бот отвечает на команды или упоминание.
